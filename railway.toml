[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run docs"

[deploy]
startCommand = "npx http-server docs-static -p $PORT -c-1"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[healthcheck]
path = "/index.html"
timeout = 300
